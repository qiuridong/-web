/**
 * App.tsx — 占位文件
 *
 * RouterProvider 模式下(见 main.tsx),App.tsx 通常不被使用,
 * 路由树由 src/app/router.tsx 描述。本文件仅作为 Vite 模板兼容性占位,
 * 防止有人误删后 IDE 报错或脚手架工具找不到。
 *
 * 后续如需顶层 Error Boundary 或 Layout,可在这里实现并由 router 调用。
 */
export default function App() {
  return null;
}
