# 签到管家 · 一键自部署指南

把整套面板部署到**你自己的一台干净 VPS**,一条命令搞定(Docker + Caddy 自动 HTTPS + 后端)。
装好是**空面板**(不含任何已有签到脚本),自己在「脚本」页上传。

> 这是部署**面板本体**。给某台 VPS 当**远程执行节点**(跑 selenium 类脚本)用的是 `agent/install.sh`,两者不同。

## 前置要求
- 一台 **Ubuntu / Debian** VPS,**root** 权限
- 一个**公网 IP**;**端口 80 + 443 放通**(签 HTTPS 必需)
- (可选)一个**域名**,A 记录解析到这台 VPS 的公网 IP
- 不需要预装 docker / node —— 脚本会自动装 docker、并在 docker 内构建前端

## 用法

```bash
# 1) 拉代码
git clone https://github.com/qiuridong/-web signin-panel && cd signin-panel

# 2) 三选一:

# 有域名(自动 Let's Encrypt HTTPS):
sudo bash install-panel.sh --access your.domain.com --email you@example.com

# 无域名、只有公网 IP —— 自动用 <ip>.sslip.io 出真 HTTPS(免买域名):
sudo bash install-panel.sh --access 1.2.3.4 --email you@example.com

# 纯 HTTP(仅内网/调试,不签证书):
sudo bash install-panel.sh --access 1.2.3.4 --http
```

装完会打印:
```
✅ 部署完成!
  访问地址 : https://your.domain.com
  管理员   : admin
  密码     : <随机生成>
  ⚠️ 登录后请立刻到「设置」修改密码!
```

## 它做了什么
1. 装 Docker(若缺)
2. 写 `.env`(DOMAIN / COOKIE_SECURE / 时区等)
3. 准备 `data/ scripts/ logs/`(**清空 scripts**,空面板起步)
4. docker 内构建前端 `dist`(免在本机装 node)
5. 构建后端镜像 + `alembic upgrade head` 建表
6. 起 `caddy`(自动 HTTPS + serve 前端 + 反代 `/api`)+ `backend`
7. 创建管理员(随机密码)并打印

## 关于 HTTPS / 无域名
- **域名**:Caddy 自动向 Let's Encrypt 申请证书。确保 DNS 已解析到本机、80/443 放通。
- **无域名只有 IP**:脚本默认用 **`<你的IP>.sslip.io`**(公共魔法 DNS,直接解析到你的 IP)→ Caddy 照样能签真证书 → 全程 HTTPS,**不用买域名**。
- **纯 HTTP**(`--http`):不签证书,仅适合内网/VPN/调试。会自动设 `COOKIE_SECURE=false`,否则浏览器不回传 Secure cookie 导致登不进去。

## 装完之后
1. 打开访问地址 → 用打印的 admin/密码登录 → **设置里立即改密码**
2. **备份主密钥**:`data/encryption.key` 是 Fernet 主密钥,丢了所有加密配置(cookie/账密)全作废 —— 立即异地离线备份(密码管理器/打印)
3. 「脚本」页上传你的签到脚本(zip / 目录),配置实例 + cron
4. (可选)「节点」页添加远程节点(用 `agent/install.sh` 部署到其它 VPS)跑 selenium 类脚本

## 常用运维(在部署目录)
```bash
docker compose -f docker-compose.install.yml logs -f backend   # 后端日志
docker compose -f docker-compose.install.yml logs -f caddy     # 证书/反代日志
docker compose -f docker-compose.install.yml ps                # 状态
docker compose -f docker-compose.install.yml restart backend   # 重启后端
docker compose -f docker-compose.install.yml down              # 停
```

更新代码后重新部署:`git pull` → `docker compose -f docker-compose.install.yml up -d --build`(前端改动需重跑第 5 步的 docker build dist)。

> ⚠️ **现有生产**(jb.aijiaxia.cc)用的是 `docker-compose.yml`(backend-only + host nginx),与本安装包的 `docker-compose.install.yml` 互不影响。
